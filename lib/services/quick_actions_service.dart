import 'package:flutter/foundation.dart';
import 'package:wassistant/utils/logger_service.dart';

/// OCPD: Structured Shortcut Management (Cleaned).
/// INTJ Strategy: This service is currently a placeholder after purging
/// the 'quick_actions' dependency to minimize artifact size.
class QuickActionsService {
  void initialize(ValueChanged<int> onAction) {
    LoggerService.i('QuickActionsService: Initialized (Stub mode).');
  }
}
